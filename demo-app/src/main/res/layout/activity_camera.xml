<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <!-- Live camera viewfinder — fills the entire screen -->
    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Top bar: close button only -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnClose"
        style="@style/Widget.MaterialComponents.Button.TextButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_margin="8dp"
        android:text="✕"
        android:textColor="@android:color/white"
        android:textSize="20sp"
        android:contentDescription="Close"
        app:rippleColor="#40FFFFFF" />

    <!-- Bottom bar: [empty] [shutter] [flip] balanced around center -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="140dp"
        android:layout_gravity="bottom"
        android:background="#99000000"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <!-- Left spacer (mirrors the flip button width for visual centering) -->
        <Space
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnShutter"
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:minWidth="0dp"
            android:minHeight="0dp"
            android:text=""
            android:contentDescription="Capture photo"
            android:backgroundTint="@android:color/white"
            app:cornerRadius="36dp"
            app:rippleColor="#40000000"
            android:elevation="4dp" />

        <!-- Flip button sits to the right of the shutter -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnFlip"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_marginStart="24dp"
            android:minWidth="0dp"
            android:minHeight="0dp"
            android:text="⇄"
            android:textColor="@android:color/white"
            android:textSize="24sp"
            android:contentDescription="Flip camera"
            android:backgroundTint="#66FFFFFF"
            app:cornerRadius="28dp"
            app:rippleColor="#40FFFFFF"
            android:elevation="4dp" />

        <Space
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

    </LinearLayout>

    <!-- Capturing overlay — shown while the SDK is taking the photo + submitting -->
    <FrameLayout
        android:id="@+id/capturingOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#CC000000"
        android:visibility="gone">

        <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:indeterminateTint="@android:color/white" />

    </FrameLayout>

</FrameLayout>
